mutation AddPrintApplication(
  $printApplication: PrintApplicationIn
  $parentId: ID
  $parentType: String
) {
  addPrintApplication(
    printApplication: $printApplication
    parentId: $parentId
    parentType: $parentType
  ) {
    ...printApplicationFull
  }
}
