import { FormulationConfiguration } from './types';

const defaultFormulationConfiguration: FormulationConfiguration = {
  // maxColorantCount: 0,
  // defaultCanSize: 100,
  sortingCriteria: [],
};

export default defaultFormulationConfiguration;
