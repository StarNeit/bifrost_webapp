export type FormData = {
  name: string;
  // version: unknown,
  // access: unknown,
  // project: unknown,
  // userNotes: unknown,
  // systemNotes: unknown,
  // tags: unknown,
  tolerance?: number;
  // type: string,
  // quality: string,
  // roughness: number,
}
